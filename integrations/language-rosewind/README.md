# language-rosewind

Atom grammar, snippets, and a run command for RoseWind.

From this repository, link the package with `apm link integrations/language-rosewind`, reload Atom, open a `.rw` file, and run **RoseWind: Run** from the command palette. The runner delegates to the shared compiler in `studio/`; install the studio dependencies first.
